"""Framework integrations for Fluq observability.

Each integration is a separate module with optional dependencies:
  pip install fluq-sdk[openai-agents]
  pip install fluq-sdk[crewai]
  pip install fluq-sdk[langchain]
"""
